# Node deps (consumers run `npm install` in marp-slides/scripts)
node_modules/

# OS / editor cruft
.DS_Store
*.log

# Marp render artifacts (decks render to .html/.pdf/.pptx; never source)
*.html
*.pdf
*.pptx

# Release zips (built by scripts/build-marpsmith-skill-zips.sh; uploaded as
# GitHub Release assets, not committed into history)
dist/
